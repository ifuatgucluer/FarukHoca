package OcaQuestions;

public class Q004 {
    public static void main(String[] args) {
        int[] stack = {10, 20, 30};
        int size = 3;
        int idx = 0;
        /* n1 */
        System.out.println("The top element: " + stack[idx]);
    }
}
    /*What is the result?
    do {    // loop steps:
    idx++;  // 1: idx = 0;  2: idx = 1,
    } while (idx <size-1);   // 1: idx=1, 2:idx=2, (2<2) is false loop ends  --> The
     */

/* Q0005 Soru 5
Which statement is true about the switch statement?

A- It must contain the default section.
B- The break statement, at the end of each block, is optional
C-Its case label literals can be changed at runtime
D- Its expression must evaluate to a collection of values
*/